{
  "name": "eers",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "serve": "lerna run serve",
    "storybook": "lerna run storybook",
    "start:client": "yarn workspace @eers/client start",
    "start:server": "yarn workspace server start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "2.0.5"
  }
}
